#!/sbin/busybox sh

_PATH="$PATH"
export PATH=/sbin

# copy files
cp -a /vendor/samsung/init.smdkc210.rc /
cp -a /vendor/samsung/init.smdkv310.rc /
cp -a /vendor/samsung/ueventd.rc /
cp -a /vendor/samsung/ueventd.smdkc210.rc /
cp -a /vendor/samsung/ueventd.smdkv310.rc /

# make init.rc
#cp -a /vendor/samsung/init.rc /init.rc

# finish mbs init
rm -fr /dev/* /misc/dev.tar
export PATH="${_PATH}"
mv /_init /init
exec /init
